<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Error al enviar usuarios</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f8d7da;
        margin: 0;
        padding: 0;
        color: #721c24;

        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }

      .email-container {
        background-color: #ffffff;
        border-radius: 8px;
        max-width: 600px;
        width: 90%;
        padding: 30px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      h1 {
        font-size: 22px;
        color: #dc3545;
        margin-bottom: 20px;
      }

      p {
        font-size: 16px;
        margin: 10px 0;
      }

      .error-details {
        background-color: #f8d7da;
        border: 1px solid #f5c6cb;
        padding: 15px;
        border-radius: 5px;
        color: #721c24;
        font-family: monospace;
        margin-top: 20px;
        white-space: pre-wrap;
        word-break: break-word;
      }

      .footer {
        margin-top: 40px;
        font-size: 12px;
        color: #6c757d;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <h1>🚨 Error al procesar el envío de usuarios</h1>

      <p>Intentamos enviar <strong><%= numUsers %></strong> usuarios al endpoint de Magneto.</p>
      <p>Ocurrió un error en el proceso relacionado con: <strong><%= field %></strong></p>

      <div class="error-details">
        <%= errorMessage %>
      </div>

      <div class="footer">
        Este es un mensaje automático del sistema. Si el problema persiste, contacta al equipo técnico.
      </div>
    </div>
  </body>
</html>
